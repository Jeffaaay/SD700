# Interpulse1 protocol and verifier contract

F10C schema /4653010F build ID /profile7. Identity/hash mismatch rejects this candidate. The unchanged50-word active ForceServo config at FC03 0x110 retains gains10/0/0, zero public continuous cap, receive lease130 ms, sample age20 ms and feedback gap125 ms. The unchanged66-word operating profile and198-word disabled candidate catalog do not authorize an independent continuous/Assist owner. Runtime plan is integer Target1..3000 with both reserved A/C slots0; full version/digest readback/ACK/arm/one-START admission is unchanged.

Immutable Build config at FC03 0x600 is now76 words (38 little-endian uint32 values serialized high Modbus word first), version3, FNV1a digest2848875769. The32 original parameters after version are byte-identical to the preceding release. Appended fields in order: preload_initial_command300, preload_min_command200, preload_max_command600, preload_step_command100, preload_drop_N2. The existing field name off_settle_ms remains30 for wire continuity; it now means cooldown after the high pulse. MICRO/FINE gaps are preloaded, COARSE gaps are OFF. No timing-only retry is authorized.

Frozen FC04 diagnostics:124 uint32 +32 float32 =312 words. Six uint32 fields are appended to the Build block: interpulse_active, interpulse_command, interpulse_next_command, interpulse_deadline_ms, interpulse_spent_ms, interpulse_credit_ms. Float offsets follow the enlarged uint32 block; schemaF10B readers must reject it. All reads keep the existing <=11-register chunks, production length parsing, CRC/station/function/exact-length/timeout checks.

interpulse_command is zero outside preload; interpulse_next_command is retained adaptive preload, and may remain nonzero while target/STOP/fault is truly OFF. Neither is an independent output request. current_committed, output_off, planned tim2/tim3 and lease_active identify current output. segment_command/mode/base/normal/hard describe the high pulse, not the gap. requested_equivalent_V and mapped_pwm_percent describe the current executor output, zero in true OFF. Reaching the preload cap is separately classified using600, without falsely treating every nonzero gap as a public continuous-cap violation.

energized_reserved_ms is nondecreasing within an epoch: nonrefundable pulse hard reservations plus prepaid preload time. interpulse_credit_ms is the unspent portion of that preload reservation, projected for the active gap. interpulse_spent_ms and energized_upper_ms include full enabled wall time and the existing one-ms conservative guard. STOP/START/new plan cannot clear those totals. A normal compare hands off while the hard ARR still protects the high pulse; preload is bounded by the original receive deadline and available prepaid time. Deadline telemetry is an MCU contract, not an oscilloscope trace.

The Python-only strict verifier checks actual ELF/HEX bounds, checksum, addressed load bytes including RAM initializers, exact hashes, literal physical arming guard, unconditional rejection of the separate continuous owner, all38 immutable Build words and all existing config/profile/catalog contracts. It requires the active Build/TIM5 handoff implementations. Developer objcopy remains an additional comparison, not a field dependency. Verifier mutations reject altered preload initial/range/step/drop values as well as every previous output/timing/safety parameter and old firmware identities. No original assertion was removed to obtain PASS.

Target reach and OFF decay do not establish active/stable HOLD. All output/current/voltage/PWM fields are software requests/register observations; winding current and heat are not measured. PHYSICAL_STATUS=NOT_RUN; HARDWARE_STOP_VALIDATION / STATIC_250 / ROTATING_LOAD remain unvalidated.
