# SD700 repository delivery policy

GitHub main is the source of truth. After validated software changes, run applicable host/script/ARM tests and strict firmware verification; retain the current field HEX/ELF, update README/current and root tracked-source SHA256 manifests, then normal commit/push origin/main and live ls-remote verification. No PR or force push. Do not generate ZIPs, archives, bundles, nested deliveries or package-verification prerequisites.

Preserve unique field evidence and historical engineering records/firmware. Inspect generated directories before removing ignored software build/test artifacts. Retain tracked test records and unique physical evidence; ignored raw software logs/final/repro outputs are rebuildable under the current cleanup policy. Never reset --hard, clean -fdx or overwrite field originals. Do not connect serial, flash or move hardware in an agent session. Software PASS is not physical validation.

Current candidate: BuildToTarget3_SourcePort1 (F10D/46530113/profile7, Build v5/digest4163791885), based on fa6fd0036ea877e4aee7d65e14f12c094b6ca478. Sole algorithm reference is the actual Field182402 working-tree pressure_control.c, SHA256 E6E08DC4E370A853EA9A586D765D97D5B6A1E7D6DDC9760202B947F99FB4D512. Original C/header live in Tests/Host/Reference/Field182402; never substitute its Git HEAD, snapshots, prior HEX or README. Direct port and deliberate differences: Docs/BuildToTarget3_SourcePort1/HANDOFF.md.

Preserve the modern machine, single executor, atomic plan/readback/digest/one START, fresh feedback, receive-anchored lease, STOP priority, fault latch, output guard and direction interlock. Source functions select commands; only MotorExecutor may drive hardware. No old driver/task, PID control or independent continuous/Assist owner is enabled.

Target250 source envelopes: continuous5000 approach before contact10 N; far final10000/12 ms; near and PrecisionForward progressively reach7000/8 ms. Do not substitute the top12 V comment for actual stage clamps. TIM5 executes the variable normal width and independent normal+1 ms hard cutoff. Pulse gaps are BRAKE (both drivers enabled, compares0), never forward preload. Source30/400 ms waits and executor validation share actual pulse end; no repeated/old frame can grow stagnation or command. New fresh frames may continue BRAKE/approach leases only, never extend high pulses.

All targets first valid reach true OFF and only monitor. No HOLD/repress/RELEASE or automatic restart. No normal overall deadline and no12 s cumulative admission gate. Persistent net-new-high>=2 N no-response budget is45 s, derived to cover the250 N source ladder plus terminal observation; START/STOP cannot erase it.8 s/40M command-ms approach is reserved before starting, with no refund;108 s verified OFF resets safety epoch without restarting. These are experimental limits, not current/thermal/stall measurements. Feedback20/125/130 ms protections and absolute3000 boundary remain.

Only integer Target1..3000 is field writable; reserved A/C slots0, gains10/0/0, PSU0.5 A unchanged. Only250 N is software-accepted this delivery. Current focused test: tools/run_force_servo_tests.py --commissioning --characterization --build-to-target --source-port --optimization O2. It compiles the full original source oracle and compares production output every1 ms. Older BuildToTarget2-specific tests are historical behavior references, not current algorithm expectations. This delivery ran O2 only, one ARM Release and one current strict verifier, no historical full suite or cleanup.

Field checkout: git switch main; git pull --ff-only; git rev-parse HEAD; python tools/verify_force_servo_firmware.py. Follow current README/hash, not historical firmware instructions. PHYSICAL_STATUS=NOT_RUN; HARDWARE_STOP_VALIDATION / STATIC_250 / ROTATING_LOAD remain unvalidated.

File organization: README/current firmware manifest are authoritative; Docs/REPOSITORY_INDEX.md separates current, historical and test-fixture uses. tools/clean_generated_artifacts.py defaults to a saved dry-run outside the checkout; --apply removes only ignored files under output after rechecking exact hashes/classification and process idle. Preserve every tracked file, nonignored output, physical/uncertain captures and explicit test/tool inputs; --keep can name a computed fixture. Ignored ARM/host builds, final/repro/focused software outputs, synthetic captures and nested clone_verification/.git are disposable. Never remove the main Git data or follow symlinks/junctions. Docs/GeneratedArtifactsCleanup1.md records the current policy; Docs/RepositoryCleanup1 and tools/cleanup_generated.py describe the earlier conservative cleanup and remain historical. The root INA240 duplicate is removed; use Reference/Hardware/ina240.pdf. Cleanup must preserve controller/configuration/firmware bytes and Git status; after verification, clean newly generated outputs again.
